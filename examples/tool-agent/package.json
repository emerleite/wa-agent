{
	"name": "wa-agent-example-tool-agent",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "AgentLoop example — books, lists, cancels appointments via Zod tools.",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"db:create": "wrangler d1 create tool-agent",
		"db:migrate": "wrangler d1 migrations apply tool-agent --local --migrations-dir ../../migrations && wrangler d1 migrations apply tool-agent --local --migrations-dir ./migrations",
		"db:migrate:remote": "wrangler d1 migrations apply tool-agent --remote --migrations-dir ../../migrations && wrangler d1 migrations apply tool-agent --remote --migrations-dir ./migrations",
		"mock:meta": "tsx ../../tools/mock-meta-server.ts"
	},
	"dependencies": {
		"@ai-sdk/google": "^2.0.0",
		"ai": "^6.0.0",
		"hono": "^4.4.4",
		"wa-agent": "file:../..",
		"zod": "^3.25.76"
	},
	"devDependencies": {
		"@hono/node-server": "^2.0.6",
		"tsx": "^4.22.4",
		"wrangler": "^4.0.0"
	}
}
